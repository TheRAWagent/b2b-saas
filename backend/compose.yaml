services:
  python-uv:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    init: true
    ports:
      - "8000:8000"  # Exposed by Dockerfile
    # env_file: ./.env  # Uncomment if .env file exists
    # Add depends_on here if you add external services below
    # networks: [appnet]  # Uncomment if you add external services below

# If you add external services (e.g., PostgreSQL, Redis), define them below and uncomment networks section above
# Example:
#  postgres:
#    image: postgres:latest
#    restart: unless-stopped
#    environment:
#      POSTGRES_USER: user
#      POSTGRES_PASSWORD: password
#      POSTGRES_DB: appdb
#    volumes:
#      - pgdata:/var/lib/postgresql/data
#    healthcheck:
#      test: ["CMD-SHELL", "pg_isready -U user"]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#    networks: [appnet]
#
#volumes:
#  pgdata:
#
#networks:
#  appnet:
#    driver: bridge
